[tool:pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --strict-config
    --verbose
    --tb=short
    --cov=shop_api
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=95
asyncio_mode = auto
